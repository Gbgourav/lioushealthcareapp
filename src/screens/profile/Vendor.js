import {View, Text} from 'native-base';

const Vendor = () => {
  return (
    <View style={{flex: 1, alignItems: 'center', justifyContent: 'center'}}>
      <Text>Vendor's dashboard</Text>
      <Text>
        Here vendor needs to fill his profile to activate this Dashboard
      </Text>
    </View>
  );
};

export default Vendor;
